services:
  proxy:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8998:8998"
    env_file:
      - path: .env
        required: false
    environment:
      RUST_LOG: ${RUST_LOG:-proxy_app=info,tower_http=info}
    volumes:
      - proxy-data:/app/data
    restart: unless-stopped

volumes:
  proxy-data:
